finish generating moves
finish selection function


? create a neural networl(something to reuse the tree that was lready generated) || rebuild on all possible moves and try to find the best possible one?
